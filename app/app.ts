/// <reference path="_Typings.ts" />

module App {
    'use strict';

    var app = angular.module(Config.Application_Namespace, [
        // Angular modules
        // Custom modules
        // 3rd party modules
    ]);

    app.config(()=> {

    });

    app.run(()=> {

    });
}